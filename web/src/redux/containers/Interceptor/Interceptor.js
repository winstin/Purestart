import React,{Component,PropTypes} from 'react';
import Tab from 'qnui/lib/tab';
import InterceptorContent from '../../components/Interceptor/Interceptor';

export default class Interceptor extends Component {
  render(){
        return(
              <InterceptorContent />
        );
    }
}

